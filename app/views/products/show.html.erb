<h1><%= @product.title %></h1>
<div class="product-images">

</div>
<div class="product-details">
  <span><%= link_to @product.url, @product.url %></span>
  <span><%= "$#{@product.price}" %></span>
  <span><%= link_to raw("<span class='glyphicon glyphicon-pencil' aria-hidden='true'></span><b class='button-text'>Delete Feed</b>"), edit_user_feed_product_path(current_user, @feed, @product), class: "btn btn-primary btn-sm delete-button"%></span>
</div>

